// GENERATED by craftsman spec gen — do not edit.
// Fully regenerated from SPEC.md on every run (this file is the CLI's, per
// the single-writer rule). Step implementations live in your own
// Steps.swift: copy Steps.swift.template next to this file, rename it, and
// implement the SpecSteps methods — craftsman never touches those files.

import Testing

@Suite("Feature: Xcode round trip")
struct XcodeRoundTripFeature {
    @Test
    func `Scenario A passes`() throws {
        var steps = SpecSteps()
        try steps.step_a_seeded_counter()
        try steps.step_the_counter_is_bumped()
        try steps.step_the_counter_holds_two()
    }

    @Test
    func `Scenario B stays undefined`() throws {
        var steps = SpecSteps()
        try steps.step_an_unwritten_step()
    }

    @Test(arguments: [
        (quantity: 3, verdict: "accepted"),
        (quantity: 12, verdict: "rejected"),
    ])
    func `Quantities within range are accepted`(quantity: Int, verdict: String) throws {
        var steps = SpecSteps()
        try steps.step_a_limit_of_10()
        try steps.step_the_quantity_is_set_to(quantity)
        try steps.step_acceptance_is(verdict)
    }

}
